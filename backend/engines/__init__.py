# Engines package: backtest, daily digest, discord bot, nexus graph, price, ai backtest, discover.
# All engines are controlled via the single RethinkDB table EngineControl (one document per engine).
