# SESSION HANDOFF — 2026-08-14b

Successor to `HANDOFF-2026-08-14.md`. Read that one for the objective and the pre-existing defect
register; read this one for what changed, because **three of its conclusions did not survive contact
with the logs.**

================================================================================
## 0. ONE-PARAGRAPH STATE
================================================================================
The conversion bug has been relocated. It is not primarily the satellite-cap trim (D1) the previous
session shipped for — that is real but cannot create a fill by construction. It is (a) the book being
ARITHMETICALLY SEALED at six names, and (b) the passive core taking every dollar of cash and holding
it across bars where its reservation refuses every alpha buy. Both are fixed behind default-OFF
flags, deployed, and armed on doc 195. A paired W0 run is in flight: control `bt 599773` (doc 194),
treatment to follow on doc 195. Goal is NOT complete and is NOT marked complete.

================================================================================
## 1. WHAT THE PREVIOUS HANDOFF GOT WRONG
================================================================================
Correct these before reasoning from that document.

**(a) "A fresh `history_scope_salt` isolates an A/B arm." FALSE.**
`_active_event_history_scope_id` (graph_nexus_analysis.py:4597-4621) hashes its OWN salt,
`active_event_history_scope_salt`. The two already-audited W0 arms shared active-event scope
`de83e7d59f26` and consumed it ASYMMETRICALLY — 17 LLM-skip cache hits against 16. Worse,
`GraphNexusDiscoverySnapshots` is keyed by BASE instance id and bootstraps precisely when the
current scope's discovery table is empty (:12930-12932) — the state a fresh salt creates — so a
fresh salt INVITES an arm to import its sibling's discovered universe. That is the most plausible
mechanism behind the AGQ artifact and the ~10pp dispersion, and it means **every prior "isolated"
pair in this project was contaminated.**
Recipe that does work: new `history_scope_salt` + new `active_event_history_scope_salt` +
`nexus_discovery_bootstrap_enabled=false` + `nexus_discovery_snapshot_enabled=false`.
Verified live on bt 599773: scope moved `4f430a0ae8cd…` -> `f34f8a9f3dc6…`, and state carried from
lookback fell from 285 trends / 132 discovered to **6 trends / 30 discovered**.

**(b) "D6: the bear leg's 70%-of-NAV park trips the brake and blocks the alpha book 19 of 21 bars."
Arithmetic CONFIRMED to the cent (102.8%); consequence REFUTED.** The brake blocked ZERO orders.
`grep -c "TURNOVER BUDGET BLOCK"` = 0, `SKIP BUY` = 0, and only two buy candidates reached the gate
all window, both before the first bind. The alpha book was starved UPSTREAM by
`Regime capacity gate (Z4.1): max_positions 6->2` with zero headroom on every cycle. Fixing D6 will
not move that run. Do not credit it with the +24.36%.

**(c) "Blocker #2: median position 4.73% of NAV." STALE.** Measured median held weight is **13.96%**
across all three windows; `total_spend_cap_concentrate` fixed it. Sizing was never the problem —
availability was. Relatedly, blocker #3 ("the satellite cap is entry-only") is wrong and the truth is
worse: it also refuses ADDS TO HELD WINNERS. 5 of 5 planned winner-adds in W0/W1 were killed by it on
the tick they were planned (L8048, L21978, L37126, L40506).

================================================================================
## 2. THE ACTUAL CONVERSION BUG (two mechanisms)
================================================================================
**(1) THE BOOK IS ARITHMETICALLY SEALED AT SIX NAMES.**
```
satellite_max_share = 1 - core_min_pct(0.10) - cash_reserve_floor_pct(0.02) = 0.88   (CONVICTION only)
satellite_design_share = 1 - core_target_pct(0.35) - cash_floor(0.02)       = 0.63   (plain buys)
per-name target = total_spend_cap_target_weight_pct                          = 0.14
6 names = 0.84  ->  room 0.04 of NAV = ~$250   vs   floor max($50, NAV*0.06) = ~$370
```
Nothing can enter and no winner can be added. Measured: satellite share median 84.67% of NAV,
**373 of 495 bars (75%) with no band clearing the floor**. At four names the plain room reopens to
$420 (exactly one plain slot) and the conviction band to $1,920. `max_positions` is too LARGE.

**(2) THE PASSIVE CORE TAKES EVERY DOLLAR AND HOLDS IT ACROSS BARS.**
`buy = min(drift_usd, _spendable)` is CASH-bound on 4 of 5 deploys — the core sat at 11.6-12.1% of
NAV against a 27-30% target all window, so the clip collapsed to every dollar available. Under
next-event execution the order pends into the next session, and its `_execution_cash_reservations`
entry is invisible to the buy gate (`get_cash()`) but binding on the executor
(`get_buying_power(reserved)`):
```
L13193 01-16  [core] deploy of $1165.72 SPY was NOT confirmed (band_deploy)
L13631 01-19  Buy gate inputs for AMZN: cash=$1299.21 reserved=$0.00 -> PASS
L13632 01-19  SKIP BUY AMZN - fundable $133.49 ... < min $379   (also SKYT, SNDK)
L14816 01-20  FILL BUY SPY qty=1.68169158
```
$1,299.21 - $1,165.72 = $133.49, exact. All five core deploys held at least one gate-passed alpha
buy hostage: $5,774.16 of core notional against $5,756.28 deployed. Refusal taxonomy over all 67
`SKIP BUY` in bt 523085: TRIM 35 ($6,759), **INFLIGHT 25 ($16,117)**, CASH 7 ($6,134); 16 of the
inflight refusals had cash at or above the floor — **$14,119 refused for want of ORDER, not capital.**

================================================================================
## 3. SHIPPED THIS SESSION (all default-OFF, deployed and verified in prod)
================================================================================
HEAD == origin/main == 32d592d, plus local-only 0b8d316 (the reader; NOT pushed — a push
auto-deploys and kills a running backtest).

| flag | what it does |
|---|---|
| `core_deploy_alpha_headroom_pct` | core leaves a fraction of NAV so the alpha book clears its floor. A CLIP, not a veto — the deploy is cash-bound, so a veto deploys the core ZERO times. Clamped at `_spendable - _min_deploy`. |
| `sleeve_park_nets_pending_cash_enabled` | sleeve park nets pending buys out of `idle`, not just `cur_val`. Fixes a measured cash double-spend. |
| `slot_exclusions_all_counters_enabled` | the FIFTH `max_positions` counter (gna.py:29522) finally honours `slot_exclusions`. Without it a cap of 4 latches a permanent BREACH and the book converges to THREE names. |

Tooling: `scripts/arm_unseal_pair.py` (arms both arms from one table and verifies the diff by
read-back), `scripts/read_unseal_pair.py` (scores both arms against all six prereg endpoints from
LOGS), `scripts/spy_benchmark.py` now prefers tick-logged `BENCHMARK QUOTE` over fills.

================================================================================
## 4. DO NOT SHIP THESE — measured, not guessed
================================================================================
- **`buy_order_conviction_ranked_enabled`** would HARM. Wired correctly, but ranks on
  `raw_net_score`, which gna.py:22180-22189 floors at a constant **1.700** for every momentum pick.
  Enabling it demotes the entire lane that finds the big movers below routine graph buys at 1.800.
  It would have changed **0 of 24** competing ticks.
- **`satellite_displacement_enabled`** is inert by three independent breaks (the trim is filtered out
  of `expanded_symbols`; it never gets the sell-signal override; its one-tick deferral lands on
  `symbols=0` ticks — 24 EXECUTE, 0 fills) AND it cost **1.12pp** in a near-clean A/B
  (bt 718107 +4.88% vs bt 523085 +6.00%).
- The **alphabetical tie-break** is not the leak: on 22 of 24 competing ticks ZERO buys funded.
- **D6 bear turnover exemption**: blocked zero orders in the window it was found in. Fix D6b first.

================================================================================
## 5. IN FLIGHT
================================================================================
Prereg: `prereg-unseal-the-book-2026-08-14b.md`. Read it before concluding anything.
- control `bt 599773` (doc 194) — RUNNING at time of writing.
- treatment (doc 195) — launch ONLY after the control reaches a terminal status. One slot; a second
  launch silently preempts the first.
- Score with `python3 scripts/read_unseal_pair.py <control> <treatment>`.
- Endpoint 1 is `grep "ALPHA HEADROOM: withheld"` and `grep satellite_cap_below_floor`. If neither
  appears the levers are inert and the run says nothing about them.

================================================================================
## 6. OPEN, NEEDS THE OPERATOR
================================================================================
- **`GraphNexusTickerHistory` is a LOOKAHEAD channel.** Keyed by bare ticker, no as-of filter on read
  (gna.py:16084-16090), feeds the sentiment prompt. A prior run over a later window leaves
  future-dated headlines that reach day-1 prompts. Nothing in the codebase purges it. It biases both
  arms identically so the PAIRED comparison survives, but **no absolute return from any run in this
  project is clean** until this is dealt with. Purging a shared production table needs sign-off.
- **Budget.** Two runs at ~$1 each this session. Prior cap was $15.
- `npx gitnexus analyze` is due — the index predates the 2026-08 turnover work and returns
  "not found" for `_turnover_is_governed`, `_core_sleeve_satellite_headroom` and several others.

================================================================================
## 7. METHOD NOTES THAT EARNED THEIR PLACE
================================================================================
- **Adversarial sweep found 7 real defects in this session's own work**, two of which would have made
  the run uninterpretable (a lane that was dead code on every document under test; a guard that left
  the alpha book under its floor on 17.3% of all 133 real deploys). Never ship a broker/core change
  without >=2 agents attacking it.
- **Smoke-test an instrument on FINISHED runs before trusting it on a live one.** The reader's first
  version reported a 0.0% funnel (a regex put `[^:]*` across a timestamp containing colons) and no
  returns at all (it searched for a "Run P&L" line that does not exist).
- **Hand-written mirror tests are worse than no tests.**
  `test_max_positions_sleeve_exclusion.py::test_all_four_counters_agree_by_construction`
  re-implements each counter inline and never calls the real ones, so it stayed green for a week
  while the real fifth counter diverged. Same anti-pattern in `test_buy_order_conviction_ranked.py`.
  Read the SOURCE.
- A **stale config backup is not the config.** A reviewer flagged doc-179 as core-enabled from an
  August 3 backup; the live document has it absent at base and in every regime profile.
